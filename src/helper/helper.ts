// import { pageFixture } from "../hooks/pagefixture";

// export async function screenshotSaver(){
//         if (pageFixture.page) {
//         const screenshot = await pageFixture.page.screenshot();
//         await this.attach(screenshot, 'image/png');
//     }
// }